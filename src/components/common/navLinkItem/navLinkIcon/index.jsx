import React from "react";
import * as Styled from "./style";

function NavLinkIcon({ element, size }) {
  return <Styled.NavLinkIcon size={size}>{element}</Styled.NavLinkIcon>;
}

export default NavLinkIcon;
